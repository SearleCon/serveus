$('#add_interaction').html("#{ j (render 'interactions/form')}");
$('body').scrollTo('#add_interaction');
$(".tm-input").tagsManager({typeaheadAjaxSource: '/tags', typeahead: true, prefilled: "#{j interaction.tag_names.join(',')}"});
$('.datetime').datetimepicker();

