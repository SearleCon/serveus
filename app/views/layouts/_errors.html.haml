- if object && object.errors.any?
  .error_explanation
    .alert.alert-error= "#{pluralize(object.errors.count, 'error')} prevented this record from being saved:"
    %ul
      - object.errors.full_messages.map do |msg|
        %li=msg

