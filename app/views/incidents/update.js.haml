$('#myModal').modal('hide');
$('body').remove('#myModal');
new_row = $("#{j render incident }")
row = $('##{dom_id(incident)}');
row.replaceWith(new_row);
new_row.children().effect('highlight', {}, 1500);
