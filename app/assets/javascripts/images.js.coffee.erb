<%
imgs = {}
Dir.chdir("#{Rails.root}/app/assets/images/") do
  imgs = Dir["**"].inject({}) {|h,f| h.merge! f => asset_path(f).chomp('/')}
end
%>

window.image_path = (name) ->
        <%= imgs.to_json %>[name]